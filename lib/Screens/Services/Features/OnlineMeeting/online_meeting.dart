import 'package:flutter/material.dart';

class OnlineMeeting extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("OnlineMeeting")),
      body: Center(child: Text("OnlineMeeting Page")),
    );
  }
}
