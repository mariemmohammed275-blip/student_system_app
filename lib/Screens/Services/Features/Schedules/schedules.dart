import 'package:flutter/material.dart';

class Schedules extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Schedules")),
      body: Center(child: Text("Schedules Page")),
    );
  }
}
